PORT=3000

# Base64-encoded contents of the Firebase Admin SDK service-account JSON file.
# Generate it with: base64 < serviceAccount.json | tr -d '\n'
# NEVER commit the JSON key, its Base64 value, or your .env file to version control.
FIREBASE_SERVICE_ACCOUNT_KEY=
