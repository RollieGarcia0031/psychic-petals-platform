PORT=3000

# Download your Firebase service account key JSON file from the Firebase Console.
# Save it in the backend directory as 'serviceAccountKey.json'.
# NEVER commit the actual JSON key file or your .env file to version control!
GOOGLE_APPLICATION_CREDENTIALS="./serviceAccountKey.json"
